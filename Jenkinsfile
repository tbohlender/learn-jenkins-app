pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                sh '''
                    echo "Build Learn Jenkins App ..."
                '''
            }
        }
    }
}
